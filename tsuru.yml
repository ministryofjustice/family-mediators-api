healthcheck:
  path: /api/v1/healthcheck
  method: GET
  status: 200
  match: .*OKAY.*
  allowed_failures: 0
  use_in_router: false
hooks:
  build:
    - sudo ln -s /usr/bin/nodejs /usr/bin/node
    - npm install && npm run build