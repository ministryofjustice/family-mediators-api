default: &default
  adapter: postgresql
  encoding: utf8
  host: localhost
  pool: 10
  timeout: 5000

development:
  <<: *default
  database: mediators_development

test:
  <<: *default
  database: mediators_test

